import PinsPage from "@/components/pins-page";

export default function Page() {
  return (
    <PinsPage
      title="Pin Transfer"
      hint="Send an unused PIN to another member by their Member ID. The PIN stays unused until it is consumed for joining."
    />
  );
}
