import PinsPage from "@/components/pins-page";

export default function Page() {
  return (
    <PinsPage
      title="Pin Used"
      hint="PINs already consumed to activate a joining (instead of a manual UTR)."
    />
  );
}
